# WIZ Intelligence Pipeline — Dependencies
# Platform: macOS Apple Silicon (M1/M2/M3), Python 3.10+
# External: brew install ffmpeg

# ── Core ──────────────────────────────────────────────────────────────────────
numpy>=1.21.0
scipy>=1.7.0
opencv-python>=4.5.0
psutil>=5.9.0

# ── Web UI ────────────────────────────────────────────────────────────────────
flask>=2.3.0

# ── Blink detection ───────────────────────────────────────────────────────────
# MediaPipe Face Mesh — runs natively on Apple Silicon CPU/Metal, no CUDA
mediapipe>=0.10.0

# ── Speech processing ─────────────────────────────────────────────────────────
# Required for transcription + speaker diarization.
# After installing, accept the pyannote licence and set HF_TOKEN:
#   https://hf.co/pyannote/speaker-diarization-3.1
#   export HF_TOKEN=hf_...
openai-whisper>=20231117
pyannote.audio>=3.1.0
torch>=2.1.0

# ── Local LLM (scene summaries) ───────────────────────────────────────────────
# Install ONE of the following backends.
# The pipeline falls back to a deterministic mock if neither is present.
#
# Option A — llama.cpp (any GGUF model, CPU/Metal):
#   pip install llama-cpp-python
#
# Option B — Apple MLX (fastest on M-series):
#   pip install mlx mlx-lm

# ── Development / testing ─────────────────────────────────────────────────────
pytest>=6.0.0
pytest-cov>=3.0.0